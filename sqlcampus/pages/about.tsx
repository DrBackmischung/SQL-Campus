import Layout from '../components/layout/Layout'

const WithStaticProps = (props: any) => {
  props;
  return (
      <Layout title="SQL-Campus | Tasks">
    
      </Layout>
  )
}

export default WithStaticProps
